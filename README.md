# @sfmc-bds/module-gamemode-area

Wave C official SFMC module: **gamemode-area**（区域游戏模式切换与背包隔离）.

## Develop

```bash
npm install
npm run typecheck
npm test
```

Install into platform:

```bash
sfmc mod install gamemode-area --from dir:. --link
```
